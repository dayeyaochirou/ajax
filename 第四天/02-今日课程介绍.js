/**
 * 1. 模板引擎 
 *      原生语法   <% if()%>
 *      简洁请求   {{ if }}
 * 
 * 2. 跨域 
 *      协议  IP地址/域名  端口号
 *      天气  百度
 *    
 *       dataType:'jsonp'
 * 
 * 3. XMLHttpRequest2.0里面的新增内容
 *      timeout
 *      process   监视上传进度  生成一个进度条
 *      FormData  可以将上传的数据转换成二进制再发送给服务器 
 * 
 * 4. 上传文件
 *      <input type="file" name="avatar">
 *       注册一个change事件   this.files[0]
 * 
 */