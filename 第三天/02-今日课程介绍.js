/**
 * 1. jQuery中的Ajax介绍
 *     主要是相关属性的使用
 *     通过一个注册案例来完全介绍使用方式
 * 
 * 2. jQuery中其它方法的使用
 *      $.get()  只能发送get请求
 *      $.post()  只能发送post请求
 *     这两个方法都只有四个参数
 *      1. url: 第1个参数是目标地址
 *      2. data: 要发送给后台的数据
 *      3. callback: 回调函数
 *      4. dataType: 要如何处理后端返回来的数据
 * 
 * 3. art-template模板  
 *      原来拼接字符串的方式太过于麻烦,不利于直观的维护 
 *      无法书写业务逻辑 
 */